// File: constants/index.ts
// Central re-exports for theme & fonts so other files import from a single stable path.
// Use this to avoid casing/path errors in imports.

export { Colors, Fonts, useThemeColors } from "./theme";
