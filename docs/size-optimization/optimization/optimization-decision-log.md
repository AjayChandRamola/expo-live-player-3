# Optimisation Decision Log

Each entry: what was decided, why (with evidence), alternatives, and how to revisit. Entries DL-01 to DL-12 were made during discovery on 2026-09-19 and are proposals until the human approves the SPEC. Status values: Proposed, Approved, Rejected, Deferred, Superseded.

| ID | Date | Decision | Evidence | Alternatives considered | Status |
|---|---|---|---|---|---|
| DL-01 | 2026-09-19 | Work in place on `main`; no feature branch or worktree | Standing user instruction; the R7 invariant test diffs Shorts against `main`, which behaves as "no uncommitted Shorts changes" when on `main` | Feature branch (rejected by instruction) | Approved (instruction) |
| DL-02 | 2026-09-19 | Fix the Jest harness (Reanimated 4 mock) before any production change | 15 suites fail to run; they are the player UI/root safety net | Proceed without them (rejected: no safety net for player-file edits) | Proposed |
| DL-03 | 2026-09-19 | Replace `@expo/vector-icons` barrel imports with deep family imports; add an ESLint guard | `IconsLazy.js` requires all 15 families eagerly; export bundles 19 TTFs (4,076,840 B); 2,412,340 B unused | (a) Metro resolver alias of the barrel to a local re-export of two families: hidden, non-obvious; (b) glyph subsetting: high maintenance (DL-06) | Proposed |
| DL-04 | 2026-09-19 | Remove expo-audio, expo-linear-gradient, expo-web-browser, @react-native-community/slider, react-native-paper, react-native-calendars, react-native-collapsible, ajv, three google-font packages; move dev tooling to devDependencies | Zero import sites; `npm ls` shows no dependents; all added in one commit (`790f900`) with a template-style feature dump | Keep "in case" (rejected: YAGNI and native footprint) | Proposed |
| DL-05 | 2026-09-19 | Do not remove expo-keep-awake, masked-view, expo-symbols, react-native-screens from the install even though unimported | They are dependencies of `expo` or `expo-router`, or (`expo-symbols`) imported by `icon-symbol.ios.tsx`; removal from `package.json` is hygiene only | `expo.autolinking.exclude` for keep-awake (rejected: dev tools use it; production benefit unmeasured and tiny) | Proposed |
| DL-06 | 2026-09-19 | Reject icon glyph subsetting | Would cut MCI from 1.3 MB to an estimated tens of KB but requires a custom icon set, generated glyph map, re-subsetting on every icon change, and diverges from `@expo/vector-icons` updates | Accept 1,664,500 B of icon fonts as the cost of maintainability | Rejected |
| DL-07 | 2026-09-19 | Keep expo-router, Reanimated, gesture-handler, expo-video, expo-image, react-native-webview | Required by product, ADR 0003, live fallback, and thumbnails; replacements violate the brief's constraints | Bare react-navigation, RN Animated, RN Image, Video via expo-av (all rejected) | Approved (constraints) |
| DL-08 | 2026-09-19 | Do not stub `@expo/ui` on Android; record as experiment X-1, off by default | `Stack.js` requires the toolbar at top level; `requireNativeModule('ExpoUI')` runs at import; stubbing means patching router internals via Metro resolver | Autolinking exclude alone (crashes); patch-package (rejected: maintenance) | Deferred |
| DL-09 | 2026-09-19 | Strip `console.log/info/debug` in production via Babel, keep `error`/`warn` | 73 call sites; observability path is `Logger.error/warn`; Shorts files are protected from edits | Edit every call site (rejected: R7, Comments type errors); custom Logger gating (partial: raw `console.*` sites remain) | Proposed |
| DL-10 | 2026-09-19 | Enable R8 and resource shrinking through `expo-build-properties`, validated by emulator playback regression | Expo template defaults leave both off; media3/webview/expo modules ship consumer keep rules | Manual `android/` edits (rejected: managed workflow; prebuild overwrites) | Proposed |
| DL-11 | 2026-09-19 | Rasterise the Shorts tab SVGs and remove react-native-svg | Only consumer besides a dead file; 104,413 B JS plus native; fixed colours make raster equivalent | Keep SVG (fallback if D-4 rejects); MCI glyph (visual change, rejected as default) | Proposed |
| DL-12 | 2026-09-19 | No device measurements in this initiative | Instruction: leave device-connected measurement for later | — | Approved (instruction) |
| DL-13 | 2026-09-19 | Report Android size as four numbers (AAB, arm64 download, armv7 download, universal APK) | One number misleads; Play delivers per-device splits | Single universal APK figure (rejected) | Proposed |
| DL-14 | 2026-09-19 | Flag, do not silently fix, the stale SDK 54 line in `CLAUDE.md` and the TypeScript 5.9/6.0 conflict | `CLAUDE.md` §2.1 asks for conflicts to be flagged | Silent edit (rejected) | Proposed (D-9) |
| DL-15 | 2026-09-19 | D-1 = `com.yagna.app` for `android.package` and `ios.bundleIdentifier`; D-2 = local Gradle for release builds | Human delegated both decisions ("take best decision yourself, all we need is to reduce the size"); no prior domain convention existed in the repo; Java 17 and the Android SDK were already present locally, giving faster iteration than a cloud EAS build for repeated Phase 0/5 measurement | EAS cloud build (rejected for iteration speed; still available via `eas.json`'s `production` profile for store artefacts) | Approved (delegated) |
| DL-16 | 2026-09-19 | D-3 approved: one-line import edits in the five Shorts files (`app/(tabs)/shorts.tsx`, `ShortActions.tsx`, `ShortCard.tsx`, `ShortsSearchBar.tsx`, `ShortVideoPlayer.tsx`) | Human explicitly granted permission; R7 invariant confirmed green after the commit landed on `main` | — | Approved |
| DL-17 | 2026-09-19 | C-08b (MaterialIcons retirement), C-09 (SVG-to-PNG Shorts icon), C-10 (blurhash placeholder) initially deferred at the start of Phase 2 | At that point in the session no emulator/rendering surface had been used yet | Apply without visual verification (rejected then) | Superseded by DL-19 once Task 16 brought up an emulator with working screenshot capture |
| DL-18 | 2026-09-19 | R8 + resource shrinking (C-14) accepted with default consumer keep rules, no `extraProguardRules` added | Release build installed on emulator; MP4 playback (progress advanced 0:00→0:16), Live tab, Shorts tab (all icons), navigation all exercised with zero `ClassNotFoundException`/`NoSuchMethodError`/`FATAL EXCEPTION` in logcat; permissions list unchanged (10/10); AAB −7.7%, arm64 download −39.3%, dex −63.9%, res −38.3% | Adding speculative keep rules pre-emptively (rejected: no failure was reproduced to justify one) | Approved; full evidence in `.size-reports/r8-regression.md` |
| DL-19 | 2026-09-19 | D-4b approved: `components/ui/icon-symbol.tsx` retired MaterialIcons for MaterialCommunityIcons (C-08b) | All 8 glyph names (`home`, `send`, `code-tags`, `chevron-right`, `access-point`, `bookmark`, `magnify`, `cog`) verified present in the installed `MaterialCommunityIcons.json` glyph map before the change, each is the standard Material Design equivalent of the MaterialIcons name it replaces, and MCI was already proven to render correctly in this exact app (Shorts action icons, `screen5-shorts.png`, Task 16). Android assets confirmed to drop 30→29 files, 2,658,869→2,302,029 B, 3→2 TTFs (MaterialIcons font gone), matching the intended effect exactly. A live emulator screenshot of the tab bar itself was attempted (dev client via `expo run:android` and a deep link to Metro) but the JS bundle did not finish loading within the session's available time (black screen, no crash, no logcat error — plausibly a slow first Metro bundle on this machine); the user explicitly authorized proceeding on the code-level evidence when an emulator step becomes a blocker | Wait indefinitely for the dev client (rejected: user authorized moving on); apply without checking glyph existence (rejected: would risk an invisible/tofu-box icon, which the glyph-map check rules out) | Approved (delegated authorization); a live visual pass on the tab bar is still recommended before a store release and is noted as unverified rather than claimed |

| DL-20 | 2026-09-19 | C-09 (SVG-to-PNG Shorts icon) and C-10 (blurhash placeholder) remain deferred, not executed | Unlike DL-19's glyph-map/semantic-equivalence check, these two changes require genuinely subjective pixel/colour comparison (rasterisation fidelity at 1x, blurhash colour/placement) that cannot be substituted with static code-level reasoning; the dev-client session needed for a live screenshot did not finish bundling within the session's available time and the user authorized moving on rather than continuing to wait | Apply without any visual check (rejected: highest risk of the three deferred items per RK-04, and the smallest wins of the remaining catalogue, −104,413 B unminified JS and −5,075 B respectively) | Deferred, pending D-4 and D-5 and a working visual-review session (device or dev-client) |

Revisit triggers: any Expo SDK upgrade (re-check DL-08 and DL-03 against the new `IconsLazy.js` and router layout files); any new icon family use (DL-03 guard will fail lint); any new dependency (dependency audit table must be updated by the same change); DL-19's tab bar icons should get a live visual pass (device or a successful dev-client session) before a store release; DL-20's items need a working visual-review session before attempting.
